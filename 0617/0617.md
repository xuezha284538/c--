# 线程与进程 -同步

## 同步与异步

异步: 多个任务 同时执行
同步:多个任务有依赖关系  有先后次序 有依赖 

## 线程同步方法
- 互斥锁
  - 
- 条件变量
- 信号量
信号量:原子性的计数器,线程安全:可以使用信号量替代互斥锁
信号:进程之间的通信的机制
 - 信号与信号量是俩种不同的概念

        类型
          - 二元信号量 0 , 1
          - 计数器信号量:
sem_init(&sem,0,1)
参数:
- 第一个信号量 
- 第二个线程间还是进程间 0为线程间  1为进程间 
- 第三个初始化信号量的值 1 为二元信号量 ,计数器信号量
    核心操作
      - p :  sem_wait()
      - 如果sem>0 获得资源  sem--
      - 如果wem=0 进入阻塞
      - v :  sem_post() 释放资源
      - sem++

- 读写锁
  - 
- 自旋锁 spin_lock
  - 与互斥锁都是排他锁,资源加锁后只能被一个线程单独使用 直到解锁 
  - 不适合单核场景为多核而生 不断轮询poll以获得锁,
  - 适用临界区比较小的场景 单核场景不适用
  - 互斥锁:单核多核都可以 未获得锁会阻塞 cpu占用低 适用临界区比较大的的场景
死锁:多个线程竞争资源时

典型问题  哲学家进餐问题




## 锁
## 作业
阅读 29-33章节 线程
售票窗口系统
 1000张
 3-4个窗口
 sleep()

 yiled()让出
