 # 数据库

## SQL 优化
- 1 索引:B+tree 3~4 叶子节点 双向链表(范围查找)   Hash
  - 自动:主键,外键,唯一约束
  - 手动:where join order by group by 
  - 类型:普通index 空间 唯一 全文 主键
  - 避免了全表扫描 回表问题
  - 影响 插入 更新 删除的性能
- 2 优化SQL语句
  - 禁用select * from
  - 避免子查询和临时表(基于执行计划)
  - join 小表()驱动大表
  - 批量写:
    - 2000 条 insert into t1 values(),(),()
    - 并入事务
  - 执行计划 
    - `explain select  `
- 3缓存
  - redis
- 4 分表
  - 表分区
  - :按范围分区
  - :按hash分区 
  - :按值分区
  - :list分区
  - 水平分表
  - 垂直分区
```sql
CREATE TABLE t1(
    id INT AUTO_INCREMENT PRIMARY KEY,
    val YEAR
)ENGINE=InnoDB
PARTITION BY RANGE (val)(
    PARTITION p0 VALUES LESS THAN (1990),
    PARTITION p1 VALUES LESS THAN (1995),
    PARTITION p2 VALUES LESS THAN (2000)
);
INSERT INTO t1(val) VALUES (1989); -- p0
INSERT INTO t1(val) VALUES (1990); -- p1
INSERT INTO t1(val) VALUES (1994); -- p1
INSERT INTO t1(val) VALUES (1995); -- p2
INSERT INTO t1(val) VALUES (1999); -- p2

creat table t1_p0
as
select * from t1 partition(p0);

```
- 5 分库
  - 读写分离:主从架构 从主库写 从库读

## Redis
功能
- 缓存服务器 key value Hash
- 数据结构的实现
- 持久化:RDB(快照) AOF(追加日志)
- 发布订阅(pub/sub)
- 事务(multi/exec/discard)
- 过期策略:定期删除 + 惰性删除
- 内存淘汰策略:noeviction allkeys-lru volatile-lru allkeys-lfu

数据类型
- string:普通缓存 计数器
- list 队列 栈
- set 无重复值 签到 抽奖
- sorted set 排行榜 排序 优先级队列 热点
- Hash  
- ...

sudo apt install redis
sudo netstat -tpnl | grep redis
## 使用
redis-cli
set xxx "" 
get xxx
incrby xxx  5

lpush list1 a b n n m
llen list


lrange list 0 -1

rpush list2 a b c d

lrange list2 0 -1


sadd s1 1 23 78 8
sadd s2 23 7 8 9 

smembers s1
srandmember s1 3
sunion s1 s2
sinter s1 s2
sdiff s1 s2

srem s1 9
spop s1 3
hset u:123 name alice age 21 tel 132
hget u:123 name
hincrby u:123 age 5
zadd top10 9 alice 8 sd 5 kom 15 xuezha
zrevrange top10 0 -1 降序
zrange top10 0 -1 升序
zincrby top10 30 tom

## http 压力测试
测试服务器程序的吞吐量 每秒接受请求的响应的次数 读写性能
nginx
多进程 +epoll


redis
- redis-benchmark -t set,get -n 100000 -c 50
- redis-benchmark -h 127.0.0.1 -p 6379 -n 100000 -c 50 -d 100

压测工具
-ab
- wrk
- wrk -t4 -c 200 -d15s http://127.0.0.1:6817


## 进程
进程:程序的一次执行过程 及持有的资源的综合:每个进程独立资源


### 多进程
- fork()
- getpid()
- getppid()
- exec 族:execl execlp execv execvp (替换当前进程映像)
- wait()/waitpid():回收子进程,避免僵尸进程
- 进程间通信(IPC):管道 消息队列 共享内存 信号 信号量 socket
### 进程类型
- 孤儿进程:父死子活,子进程 ppid =1 systemd 系统守护进程
- 僵尸进程:父活子死 子进程资源未回收,子进程的状态变为 Z
- 守护进程:后台运行,脱离控制终端,ppid=1,会话首进程
  - 创建步骤:
    1. fork() 父进程退出,子进程后台运行
    2. setsid() 创建新会话,脱离原控制终端
    3. 再次 fork()(可选) 防止重新打开终端
    4. chdir("/") 改变工作目录到根
    5. umask(0) 重设文件权限掩码
    6. close(0/1/2) 关闭标准输入输出,重定向到 /dev/null
  - 常见守护进程:sshd crond nginx redis mysqld
ps -ef | grep abc 

### 子进程独立任务
- execl:list
- execv:array
- execlp

```c
#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>
#include<sys/wait.h>
// unix std posix 可移植接口规范
int main()
{
    int pid = fork();
    if (pid == -1)
    {
        perror("创建失败");
        return EXIT_FAILURE;
       
    }
    if (pid == 0)
    {
        // 子进程空间
        printf("worker:%d\t%d\n",getpid(),getppid());
        // sleep(30);
        //exec 
        //execl 多个参数逗号分隔
        //execv 数组
        //execlp("wc","wc","-l","p2.c",NULL);

        //1.路径
        //2.程序名
        //3.参数
        //4.NULL 标识参数结束
        // execlp("uname", "uname", "-r", NULL);
        execlp("./m","m",NULL);
        exit(42);
    }
    else
    {
        //主进程
        
        int status;
        waitpid(pid,&status,WUNTRACED);
        if(WIFEXITED(status))
        {
            int val=WEXITSTATUS(status);
            printf("val=%d\n",val);
        }
        printf("master:%d\t%d\n", getpid(), getppid());
        printf("pid:%d\n",pid);
        // sleep(90);
    }

    return 0;
}


```
## 任务 :ch 37 创建守护进程 ch   33进程
  sshd,mysqld,httpd,xxxd