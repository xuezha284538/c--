# 数据库 - 视图函数 存储过程 C API

## 回顾
- 关系范式:1NF 2NF 3NF
- 表之间的关系 
    - 一对多  1:n(外键)
    - 多对多  n:m(中间表)
    - 一对一(特殊的一对多 共享主键 last_insert_id() 序列 currval() nextval())
- 表连接 
    - 内连接 等值连接
    - 外连接
      -  左外连接 
      -  右外连接
      -  全连接
- 事务
  - A 原子性: 多个数据操作
  - C 一致性: 事务执行前后的数据
  - I 隔离性: 多个事务之间
  - D 持久性: 事务提交后 写入磁盘
- 
- 索引
  - 主键 唯一 全文 默认 空间 位图索引 :低基数列
  - 技术实现 B+树 hash R树
  - 主键 唯一约束 外键(join)  这些关键字后面的条件可以建立索引 where order by groupby
  - 

## 视图.
  虚表,存储在数据库中 SQL 查询语句

  表:数据的存储结构,
  视图:数据的逻辑结构 业务结构
  视图里的数据能否更新和删除?
  分情况 
  - 数据来自一张表 而且没有使用函数,都是原始的数据 可以更新和删除和插入
  - 数据来自多张表或者使用函数 不可以 更新,插入,删除

    ```sql
    
    --dept,staff
     select s.id,s.name,d.name,s.salary
     from
     staff s
     left join
     dept d
     on
     dept_id=d.id
     ;

create view v_staff
as
  select s.id,s.name  ,d.name dept,s.salary
     from
     staff s
     left join
     dept d
     on
     dept_id=d.id
     ;

   
      create view v_dept_n
     as
  select d.id,d.name  ,count(s.id) n
     from
     dept d
     left join
     staff s
     on
     dept_id=d.id
     group by d.name,d.id
     ;
    ```
## 函数

内置函数:
自定义函数:

```sql
select id,name,salary,bouns,
设置全局参数 信任函数创建者

set global log_bin_trust_function_creators=ture;


CREATE DEFINER=`TOM`@`%` FUNCTION `income`(salary decimal) RETURNS int
BEGIN

RETURN ifnull(salary,0)*3;
END


```

## 存储过程 触发器            
- 过程:存储过程 
- 命名 封装 sql 代码块
- 调用者,通过过程名,隐藏表结构 业务流程

```sql
存储过程
CREATE DEFINER=`TOM`@`%` PROCEDURE `reg`(name varchar(16),passwd varchar(64))
BEGIN

insert into user(`name`) values(name);
insert into logg values(last_insert_id(),"jcak",sha2(passwd,256));


END
-- 调用
call reg("1123","56");

```
## SQL 优化


 ### 核心数据(结构体)
 - MYSQL  数据库连接
 - MYSQL_RES 执行sql 获得资源 结果集
 - MYSQL_ROW 从结果集获取一行数据 记录 字符串数组
 - MYSQL_FIELD 列 字段的 元数据 名字 类型 大小
 - `
### 函数
- mysql_init();
- mysql_real_connect()
- 
- mysql_close()
- mysql_query()
- mysql_stone_result()
- mysql_fetch_row()
- mysql_fetch_filed()
- mysql_close_res()

##  MYSQL C API
c api 库

sudo apt install libmysqlclient-dev
cd /usr/include/mysql --头文件
cd /usr/lib/mysql     --库文件 xxx.so shared object 共享库    xxx.dll 动态连接库(windows)
```c



```
## 如何避免SQL 注入
mysql_stmt
mysql_bind
mysql_stmt_init

mysql_stmt_close
mysql_stmt_init


- 预编译语句
 优点
 1. 语句在数据库引擎中提前编译好,每次调用直接执行
 2. sql 不进行拼接,字符串 避免sql注入
```c

char *sql="select id, name,dept from dept where id=?"
char *sql="insert into  dept(name,dept) values(?,?) ";


```