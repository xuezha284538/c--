# C语言学习总结（1118-0115）

## 学习路径概览

从11月18日到1月15日，学习内容涵盖了C语言的完整基础知识体系，遵循了从简单到复杂的学习路径：

### 第一阶段：基础入门（1118-1129）
- **1118**: 基础编程入门，Hello World程序
- **1122**: 数据类型和编译过程
- **1125**: 数字处理程序
- **1129**: 运算符和表达式

### 第二阶段：数据结构基础（1202-1220）
- **1202**: 运算符进阶
- **1209**: 数组和位操作
- **1219**: 位操作深入
- **1220**: 字符和字符串处理

### 第三阶段：高级编程概念（1223-0115）
- **1223**: 函数定义和模块化
- **1231**: 指针基础
- **0107**: 指针进阶
- **0109**: 指针和字符串
- **0110**: 字符串处理和任务练习
- **0112**: 模块化编程
- **0113**: 字符串和周期处理
- **0114**: 头文件和模块化
- **0115**: 综合项目实践

## 核心知识点与代码示例

### 1. 基础语法与编译

**学习时间**: 1118-1122

**核心内容**:
- C语言基本结构（main函数、printf输出）
- 数据类型（int、char、float等）
- 编译过程（预处理、编译、汇编、链接）
- 基本输入输出（printf、scanf）

**示例代码** (来自1118/hello.c):
```c
#include <stdio.h>

int main ()
{
    printf("hello gcc");
    return 0;
}
```

### 2. 运算符与表达式

**学习时间**: 1125-1202

**核心内容**:
- 算术运算符（+、-、*、/、%）
- 关系运算符（>、<、>=、<=、==、!=）
- 逻辑运算符（&&、||、!）
- 位运算符（&、|、^、~、<<、>>）
- 赋值运算符和复合赋值

**示例代码** (来自1219/bit.c - 位交换算法):
```c
#include <stdio.h>
int main()
{
    int x=2, y=3;
    x=x^y; // 010 011  001
    y=x^y; // 001 011  010
    x=x^y; // 001 010  011
    printf("交换后的值为:x=%d,y=%d\n", x, y);
    return 0;
}
```

### 3. 数组与字符串

**学习时间**: 1209-0110

**核心内容**:
- 一维数组定义和使用
- 字符串表示和处理
- 字符串库函数（strlen、strcpy、strcmp等）
- 字符数组操作

**示例代码1** (来自1209/array.c - 成绩等级判定):
```c
#include <stdio.h>
int main(int argc, char const *argv[])
{ //    声明数组变量
    char grade[5] = {'E', 'D', 'C', 'B', 'A'};
    int n[5] = {60, 70, 80, 90, 100};
    int m = 0;
    printf("请输入成绩:");
    scanf("%d", &m);
    for (int i = 0; i < 5; i++)
    {
        if (m > n[i])
            continue;

        else
        {
            printf("成绩等级为:%c\n", grade[i]);
            break;
        }
    }

    return 0;
}
```

**示例代码2** (来自0109/str.c - 字符串处理):
```c
#include<stdio.h>
#include<string.h>

int main(int argc, char const *argv[])
{
    char a[]={'h','e','l','l','o'};
    char b[]={"hello"};
    char d[]={"bye"};
    char c[]={'h','e','l','l','o','\0'};
    char *j="hello";
    j="bye";

    b[0]=d[0];
    b[1]=d[1];
    b[2]=d[2];
    b[3]=d[3];

    printf("%s\n",a);
    printf("%s\n",b);
    printf("%s\n",c);
    printf("%s\n",j);
    return 0;
}
```

### 4. 函数与模块化编程

**学习时间**: 1223-0114

**核心内容**:
- 函数定义和声明
- 函数参数和返回值
- 头文件使用（.h文件）
- 模块化设计
- 函数调用和递归

**示例代码** (来自1223/fun.c - 斐波那契数列和最大公约数):
```c
#include<stdio.h>
#include<math.h>
#include "my.h" //当前路径的my.h头文件

// 斐波那契数列递归实现
int fib_recursive(int n) {
    if (n == 0) return 0;  // 基情况：F(0) = 0
    if (n == 1) return 1;  // 基情况：F(1) = 1
    return fib_recursive(n-1) + fib_recursive(n-2);  // 递归公式
}

// 斐波那契数列循环实现
int fib_iterative(int n) {
    if (n == 0) return 0;  // 特殊情况：F(0) = 0
    if (n == 1) return 1;  // 特殊情况：F(1) = 1
    int a = 0, b = 1, c;  // a=F(0), b=F(1)
    for (int i = 2; i <= n; i++) {  // 从 i=2 开始循环到 n
        c = a + b;  // 计算下一项
        a = b;      // 更新 a 为前一项
        b = c;      // 更新 b 为当前项
    }
    return b;  // 返回 F(n)
}

// 最大公约数递归实现
int gcd_recursive(int a, int b) {
    if (b == 0) return a;
    return gcd_recursive(b, a % b);
}

int main(int argc, char const *argv[])
{
    printf("最大公约数是%d\n", gcd_recursive(9,108));
    return 0;
}
```

### 5. 指针

**学习时间**: 1231-0109

**核心内容**:
- 指针概念和定义
- 指针与数组关系
- 指针运算
- 指向指针的指针
- 指针作为函数参数

**示例代码** (来自1231/pointer.c - 指针交换函数):
```c
#include <stdio.h>

/// @brief pointer.c
//值 :基本类型 复制一份
void swap(int *n, int *m)
{
    int temp = *n;
    *n = *m;
    *m = temp;
}

int main(int argc, char const *argv[])
{
    char c = 'a';
    int a = 1;
    double g = 8.0;

    char *p2 = &c;
    int *p1 = &a;
    double *p3 = &g;

    printf("大小(c)=%zu\n", sizeof(c));
    printf("大小(a)=%zu\n", sizeof(a));
    printf("大小(g)=%zu\n", sizeof(g));
    printf("指针大小(p2)=%zu,指针的值%p,指向的空间数据%c\n", sizeof(p2), p2, *p2);
    printf("指针大小(p1)=%zu,指针的值%p,指向的空间数据%d\n", sizeof(p1), p1, *p1);
    printf("指针大小(p3)=%zu,指针的值%p,指向的空间数据%lf\n", sizeof(p3), p3, *p3);
    return 0;
}
```

### 6. 综合项目实践

**学习时间**: 0115

**核心内容**:
- 日期处理程序
- 斐波那契数列实现
- 综合项目开发

**示例代码** (来自0115/day.c - 日期计算程序):
```c
#include <stdio.h>

int days(int year, int month, int day)
{
    int today, temp = 0;
    int month2[month];
    
    for (int i = month - 1; i > 0; i--)
    {
        switch (i)
        {
        case 1:
        case 3:
        case 5:
        case 7:
        case 8:
        case 10:
        case 12:
            month2[i] = 31;
            break;

        case 4:
        case 6:
        case 9:
        case 11:
            month2[i] = 30;
            break;

        case 2:
            if ((year % 4 == 0) && (year % 100 == 0) || year % 400 == 0)
            {
                month2[i] = 29;
            }
            else
            {
                month2[i] = 28;
            }
            break;
        default:
            break;
        }
        temp += month2[i];
    }
    if (month == 1)
    {
        month2[month - 1] = 0;
    }

    today = temp + day;
    return today;
}

int main(int argc, char const *argv[])
{
    int year;
    int month;
    int day;
    printf("请输入年份月份和天数(2006,1,3): ");
    scanf("%d,%d,%d", &year, &month, &day);
    printf("天数:%d", days(year, month, day));
    return 0;
}
```

## 学习建议

1. **循序渐进**: 按照文件夹顺序复习，从基础到高级
2. **动手实践**: 运行每个示例代码，观察输出结果
3. **理解原理**: 重点理解指针、数组、函数等核心概念
4. **模块化思维**: 学习如何将复杂问题分解为小模块
5. **项目实践**: 通过0115的综合项目巩固所学知识

## 复习重点

1. **基础语法**: 数据类型、运算符、控制结构
2. **数据结构**: 数组、字符串的处理方法
3. **函数设计**: 参数传递、返回值、递归
4. **指针应用**: 指针与数组、指针作为函数参数
5. **模块化**: 头文件使用、代码组织

通过系统复习这些内容，可以全面掌握C语言编程的基础知识，为后续学习更高级的编程概念打下坚实基础。