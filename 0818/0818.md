内核定制：
内核源代码：www.kernel.org
内核源代码目录结构：将功能相关的文件组织在相同的目录
china@ubuntu:/usr/src/linux-source-5.0.0/linux-source-5.0.0$ ls
arch           drivers      Kconfig        Module.symvers  tools
block          dropped.txt  kernel         net             ubuntu
certs          firmware     lib            README          update-version-dkms
COPYING        fs           LICENSES       samples         usr
CREDITS        include      MAINTAINERS    scripts         virt
crypto         init         Makefile       security        vmlinux-gdb.py
debian.master  ipc          mm             snapcraft.yaml
Documentation  Kbuild       modules.order  sound
arch: 根据CPU体系结构的不同而区分的代码
crypto: 加密、压缩、CRC校验算法
Document: 内核文档
driver: 设备驱动程序源代码。我们自己定制的设备驱动可以放到该目录的特定的分类中，随操作系统一起编译
fs: 和文件系统有关的源代码
include: 内核所需要的头文件。我们自己实现驱动程序的时候使用的就是该目录下的头文件(不能使用C库中的)，和平台无关的头文件在include/linux目录
kernel: Linux内核核心部分代码(完成操作系统核心功能部分的代码)
lib: 库文件相关代码(和平台相关的)
mm: 实现内存管理的源代码，和硬件体系架构无关的部分(和硬件体系结构相关的部分在arch中)
net: 网络协议栈
scripts: 配置内核的脚本
usr: 和用户相关的文件，gen_init_cpio.c是cpio命令的实现(制作根文件系统的命令 -- 将根文件系统和内核组织在一起)

Linux内核配置和编译:
为了简化内核源代码的编译，一般采用以下的机制:
Makefile: 根据配置的情况，构造出需要编译的源文件列表，然后分别编译，并将目标代码链接到一起，最终形成linux内核的二进制文件。因为内核源代码按照功能组织的树形结构，所以Makefile文件也是分布在目录树形结构中的不同的目录下(Makefile文件具有完整的依赖链)
Kconfig: 为用户提供的层次化的配置选项集。在执行make menuconfig的时候会通过分布在各个子目录中的Kconfig文件来构建用户界面
.config: 内核配置的结果，用户配置完成以后会将所有的配置信息保存到该文件中

编译流程：
make menuconfig   配置
make -j4          编译
编译完成以后会生成bzImage  -- 内核映像文件(如果使用gcc编译，可以在本机使用，如果使用交叉编译器编译就可以下载到开发板上使用)

make clean
make distclean
可以用来删除编译过程中生存的中间文件、配置文件

文件系统的定制
操作系统中负责管理和存储文件信息的软件机构称为文件管理系统，简称文件系统。文件系统由三部分组成：与文件管理有关的软件、被管理的文件以及实施文件管理所需的数据结构。从系统角度来看，文件系统是对文件存储器空间进行组织和分配，负责文件的存储并对存入的文件进行保护和检索的系统。具体地说，它负责为用户建立文件，存入、读出、修改、转储文件，控制文件的存取，当用户不再使用时撤销文件等。

根文件系统目录
/bin
必备的用户命令
/sbin
必备的系统管理员命令
/dev
设备文件
/etc
系统配置文件，包括启动文件
/lib
必要的链接库，例如C链接库、内核模块
/home
用户主目录
/root
root用户主目录
/usr
    第二层包含多大多数用户都有用的大量应用程序和文件
    /usr/bin     非必备的用户程序
    /usr/sbin   非必备的管理员程序
    /usr/lib       库文件
/var
监控程序和工具程序所存放的可变数据
/proc
用来提供内核与进程信息的虚拟文件系统
/mnt
文件系统挂接点，用于临时安装文件系统
/tmp
暂时性的文件

init进程启动系统的过程
代码在busybox的init/init.c中
初始化阶段
	设置信号处理函数
	初始化控制台
	解析inittab
执行inittab命令
	执行SYSINIT命令
	执行WAIT命令
	执行ONCE命令
	执行RESPAWN命令
	执行ASKFIRST命令
检测RESPAWN和ASKFIRST命令，有退出的则重新执行

文件系统的定制:
1. 建立根文件系统目录
#!/bin/sh
echo "------Create rootfs directons start...--------"
mkdir rootfs
cd rootfs
echo "--------Create root,dev....----------"
mkdir root dev etc boot tmp var sys proc lib mnt home
mkdir etc/init.d etc/rc.d etc/sysconfig
mkdir usr/sbin usr/bin usr/lib usr/modules
echo "make node in dev/console dev/null"
mknod -m 600 dev/console c 5 1
mknod -m 600 dev/null c 1 3
mkdir mnt/etc mnt/jffs2 mnt/yaffs mnt/data mnt/temp
mkdir var/lib var/lock var/run var/tmp
chmod 1777 tmp
chmod 1777 var/tmp
echo "-------make direction done---------

2. 建立动态链接库
如果有开发板提供的现成的，直接拷贝；如果没有，逐一找到开发板运行环境需要的。
拷贝到新建的根文件目录lib 内。

3. 交叉编译Busybox
Busybox是一个遵循GPL v2 协议的开源项目，它在编写过程总对文件大小进行优化，并考虑了系统资源有限(比如内存等)的情况，使用Busybox可以自动生成根文件系统所需的bin、sbin、usr 目录和linuxrc文件。

4. 建立etc目录下的配置文件
(1). 新建etc/mdev.conf文件，内容为空.
(2)、拷贝主机etc 目录下的passwd、group、shadow 文件到rootfs/etc 目录下。
(3)、etc/sysconfig 目录下新建文件HOSTNAME，内容为"newer"。
(4)、etc/inittab 文件：
参考文件内容：
#etc/inittab
::sysinit:/etc/init.d/rcS
s3c2410_serial0::askfirst:-/bin/sh
::ctrlaltdel:/sbin/reboot
::shutdown:/bin/umount -a –r

5、etc/init.d/rcS 文件：
参考文件内容
#!/bin/sh
PATH=/sbin:/bin:/usr/sbin:/usr/bin
runlevel=S
prevlevel=N
umask 022
export PATH runlevel prevlevel
echo "----------mount all----------------"
mount -a
echo /sbin/mdev>/proc/sys/kernel/hotplug
mdev -s
Echo "******************************************"
echo "*******Studying ARM***********"
echo "Kernel version:linux-2.6.32.2"
echo "Student:newer"
echo "Date:2010.8.21"
echo "***********************************************"
/bin/hostname -F /etc/sysconfig/HOSTNAME

6. etc/fstab 文件
#device	mount-point   type  option   dump	fsck      order
proc	/proc		proc	 default    0         0
none	/tmp		ramfs	 default    0         0
sysfs	/sys		sysfs	 default    0         0
mdev	/dev		ramfs	 default    0         0

7. etc/profile
#A sh profile

USER="newer"
LOGNAME=$USER
PS1='[\u@\h\W]\$'
PATH=$PATH
HOSTNAME='/bin/hostname'
export USER LOGNAME PS1 PATH

7. 生成根文件系统的映像
使用以下命令安装好yaffs文件系统制作工具：
cd /home
tar –zxvf mkyaffs2image.tgz –C /
在/home 目录下，使用命令
mkyaffs2image-128M rootfs rootfs.img 
生成根文件系统映像文件。


驱动程序
驱动是一个存在于应用程序和实际设备间的软件层,是操作系统内核和机器硬件之间的接口。
设备驱动程序是内核的一部分，主要完成以下功能：
对设备初始化和释放   -- 和操作系统的交互(操作系统识别设备)
把数据从内核传送到硬件和从硬件读取数据 -- 设备和操作系统的数据交互
读取应用程序传送给设备文件的数据和回送应用程序请求的数据 -- 设备文件和设备的交互
检测和处理设备出现的错误 -- 异常处理

由于MMU的内存映射以及对操作系统的保护，运行在用户态的应用程序一般不能直接访问硬件的地址。因此需要驱动程序作为应用程序和访问硬件之间的接口

按照对用户空间的接口，Linux操作系统的驱动程序分为三种基本的类型：
字符设备：由文件系统管理
块设备：由文件系统管理
网络设备：由协议栈管理
其他设备
在用户空间，通过设备文件访问字符设备和块设备，通过socket访问网络设备。


china@ubuntu:~$ ll /dev
crw-------   1 root  root     10, 175 8月  18 09:34 agpgart
crw-r--r--   1 root  root     10, 235 8月  18 09:34 autofs
crw-------   1 root  root      5,   1 8月  18 09:34 console
brw-rw----   1 root  disk      2,   0 8月  18 09:34 fd0
crw-rw-rw-   1 root  root      1,   7 8月  18 09:34 full
crw-rw-rw-   1 root  root     10, 229 8月  18 09:34 fuse

设备文件的属性有三部分信息组成：
第一部分是文件的类型，值可能是c/b
第二部分是设备的主设备号
第三部分是设备的次设备号
设备类型和主设备号结合在一起唯一的确定了设备文件的驱动程序及其界面，而次设备号则说明目标设备是同类设备中的第几个。

利用proc下的devices文件，取得当前字符设备和块设备的信息：
china@ubuntu:~$ cat /proc/devices 
Character devices:
  1 mem
  4 /dev/vc/0
  4 tty
  4 ttyS
  5 /dev/tty
  5 /dev/console
  5 /dev/ptmx
  5 ttyprintk
  6 lp
  7 vcs
 10 misc
 13 input
 14 sound/midi
 14 sound/dmmidi
 21 sg
 29 fb
 89 i2c

Block devices:
  2 fd
  7 loop
  8 sd
  9 md
 11 sr
 65 sd
 66 sd
 67 sd
 68 sd
 69 sd
 70 sd

Sys文件系统是Linux内核中设计较新的一种虚拟的基于内存的文件系统，它的作用与proc有些类似，除了与proc相同的具有查看和设定内核参数功能之外，还有为Linux统一设备模型作为管理之用。
Sys文件系统只有读和写两个接口，比较简单，不支持复杂的操作
Sys文件系统被挂接在Linux文件系统的/sys目录中
查看系统所支持的各种休眠模式的命令：
$ cat /sys/power/state
china@ubuntu:~$ cat /sys/power/state
freeze standby mem disk

控制进入休眠模式的命令：
$ echo standby > /sys/power/state

内核通过 printk() 输出的信息具有日志级别，日志级别是通过在 printk() 输出的字符串前加一个带尖括号的整数来控制的，如 printk("<6>Hello, world!/n");。内核中共提供了八种不同的日志级别，在 linux/kernel.h 中有相应的宏对应。
#define KERN_EMERG ?"<0>" /* system is unusable */
#define KERN_ALERT "<1>" /* action must be taken immediately */
#define KERN_CRIT  "<2>" /* critical conditions */
#define KERN_ERR  "<3>" /* error conditions */
#define KERN_WARNING  "<4>" /* warning conditions */
#define KERN_NOTICE  "<5>" ?/* normal but significant */
#define KERN_INFO  "<6>" /* informational */
#define KERN_DEBUG  "<7>" /* debug-level messages */


驱动程序测试:
//TestDriver0810.c
#include <linux/init.h>
#include <linux/fs.h>
#include <linux/types.h>
#include <linux/kernel.h>
#include <linux/module.h>

MODULE_LICENSE("Dual BSD/GPL");

//设备初始化：完成设备硬件的初始化，并向操作系统注册该设备
static int test_init(void)
{
	//设备硬件初始化
	//设备的注册
	printk(KERN_ALERT"Hello, My first driver login\n");  //内核中的输出函数
	return 0;
}

//设备的注销: 释放设备所占用的资源，从操作系统中注销设备
static void test_exit(void)
{
	//设备所用资源的释放
	//设备的注销
	printk(KERN_ALERT"Bye, My first driver exit\n");
}

module_init(test_init);
module_exit(test_exit);

#Makefile文件
ifeq ($(KERNELRELEASE),)

KERNELDIR ?= /lib/modules/$(shell uname -r)/build M=$(PWD) modules
PWD := $(shell pwd)

modules:
	$(MAKE) -C $(KERNELDIR) M=$(PWD) modules

modules_install:
	$(MAKE) -C $(KERNELDIR) M=$(PWD) modules_install

clean:
	rm -rf *.o *~ core .depend .*.cmd *.ko *.mod.c .tmp_versions

.PHONY: modules modules_install clean

else
    obj-m := TestDriver0818.o
endif

china@ubuntu:~/Test20260818$ make
make -C /lib/modules/5.4.0-150-generic/build M=/home/china/Test20260818 modules M=/home/china/Test20260818 modules
make[1]: 进入目录“/usr/src/linux-headers-5.4.0-150-generic”
  CC [M]  /home/china/Test20260818/TestDriver0818.o
  Building modules, stage 2.
  MODPOST 1 modules
  CC [M]  /home/china/Test20260818/TestDriver0818.mod.o
  LD [M]  /home/china/Test20260818/TestDriver0818.ko
make[1]: 对“modules”无需做任何事。
make[1]: 离开目录“/usr/src/linux-headers-5.4.0-150-generic”
china@ubuntu:~/Test20260818$ ls
built-in.a  modules.order   TestDriver0818.c   TestDriver0818.mod    TestDriver0818.mod.o
Makefile    Module.symvers  TestDriver0818.ko  TestDriver0818.mod.c  TestDriver0818.o

编译生成的TestDriver0818.ko就是驱动程序；
在使用该驱动程序的时候需要使用insmod命令手动加载
卸载使用rmmod
china@ubuntu:~/Test20260818$ sudo insmod ./TestDriver0818.ko
[sudo] china 的密码：

insmod的时候，会调用驱动程序中使用module_init()指定的test_init函数，在该函数中调用的printk输出的内容输出到了日志文件中，可以使用dmesg命令查看
china@ubuntu:~/Test20260818$ dmesg
在最后可以看到新增加的日志信息:
[24861.516462] Hello, My first driver login

也可以使用lsmod查看当前系统中加载的驱动程序:
china@ubuntu:~/Test20260818$ lsmod
Module                  Size  Used by
TestDriver0818         16384  0
rfcomm                 81920  4
intel_rapl_msr         20480  0
bnep                   24576  2

使用rmmod卸载驱动程序的时候，会调用驱动程序中通过module_exit()指定的test_exit函数
china@ubuntu:~/Test20260818$ sudo rmmod TestDriver0818


```c
#include <linux/kernel.h>
#include <linux/fs.h>
#include <linux/init.h>
#include <linux/module.h>
#include <linux/types.h>

MODULE_LICENSE("Dual BSD/GPL");

//设备初始化 完成设备硬件初始化 并向操作系统注册该设备
static int test_init(void)
{
    printk(KERN_ALERT "HELLO,MY FIRST DRIVER LOGIN\n");
    return 0;
}

//设备的注销 释放设备所占用的资源 从操作系统中注销设备
static void test_exit(void)
{
    //内核中的输出函数
    printk(KERN_ALERT "bye,my first driver exit\n");
}

module_init(test_init);
module_exit(test_exit);

```

