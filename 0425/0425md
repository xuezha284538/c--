1# 图论基础

## 一、最小生成树（Minimum Spanning Tree, MST）

### 1. Prim 算法
**核心思想**：从一个顶点开始，每次选择与当前顶点集合距离最近的顶点加入集合，直到所有顶点都被加入。

**时间复杂度**：O(V²) 或 O(E log V)（使用优先队列优化）

**适用场景**：稠密图 

### 2. Kruskal 算法
**核心思想**：将所有边按权重排序，依次选择权重最小的边，若该边不会形成环则加入生成树，直到所有顶点连通。

**时间复杂度**：O(E log E)

**适用场景**：稀疏图

**算法步骤**：
1. 将所有边按权重从小到大排序
2. 初始化并查集
3. 依次遍历每条边：
   - 如果边的两个顶点不在同一集合，则合并
   - 否则跳过（避免形成环）
4. 重复直到所有顶点连通

---

## 二、单源最短路径（Single-Source Shortest Path）

### 1. Dijkstra 算法

**核心思想**：贪心策略，每次选择距离源点最近的未访问顶点，更新其邻接点的距离。

**限制**：边权必须非负

**时间复杂度**：O(V²) 或 O(E + V log V)（使用优先队列优化）

**算法步骤**：
1. 初始化距离数组 dist[]，源点距离为 0，其余为无穷大
2. 初始化访问标记数组 visited[]
3. 重复 V 次：
   - 选择未访问的距离最小的顶点 u
   - 标记 u 为已访问
   - 对于 u 的每个邻接点 v：
     - 如果 dist[v] > dist[u] + weight(u, v)，则更新 dist[v]

**示例：Dijkstra 执行过程**

```
顶点: 0~6，源点: 0

迭代过程:
    顶点   min距离   访问   前驱路径
   ─────────────────────────────────
0    0       0        T
1    4       4        T        0
2    5       5        T        1
3    6       6        T        0
5    9       9        T        2
4   10      10        T        5
6   16      16        T        4

最短路径: 0~6 = 16
路径: 0 → 1 → 2 → 5 → 4 → 6
```

**另一个示例**：

```
顶点: 0~5，源点: 0

    顶点   min距离   前驱路径   访问
   ──────────────────────────────────
0    0         0                   T
4    1        30          0        T
2    2        10          1        T
1    3        -                    T
3    4        50          4        T
5    5        60          3        T

最短路径:
0~2 = 10
0~4 = 30
0~3 = 50
0~5 = 60
路径: 0 → 4 → 3 → 5
```

### 2. Bellman-Ford 算法

**核心思想**：通过松弛操作逐步逼近最短路径，可处理负权边。

**时间复杂度**：O(VE)

**检测负权环**：如果进行 V-1 次松弛后，仍能更新距离，则存在负权环。

### 3. Floyd-Warshall 算法

**核心思想**：动态规划，计算所有顶点对之间的最短路径。

**时间复杂度**：O(V³)

**适用场景**：小规模图，需要计算所有顶点对的最短路径

---

## 三、拓扑排序（Topological Sorting）

**核心思想**：对有向无环图（DAG）的顶点进行排序，使得对于每条有向边 u→v，u 在排序中出现在 v 之前。

**算法步骤**（Kahn 算法）：
1. 计算所有顶点的入度
2. 将入度为 0 的顶点加入队列
3. 从队列中取出顶点 u：
   - 将 u 加入拓扑序列
   - 对 u 的每个邻接点 v，入度减 1
   - 如果 v 的入度变为 0，加入队列
4. 重复直到队列为空

**时间复杂度**：O(V + E)

**应用场景**：任务调度、课程安排 、编译顺序等

**示例**：
```
DAG 图:
A → B → D
A → C → D

拓扑排序结果（可能有多种）:
A → B → C → D
A → C → B → D
```

---

## 四、图的遍历

### 1. 深度优先搜索（DFS）

**核心思想**：沿着路径尽可能深地探索，直到无法继续，然后回溯。

**实现方式**：递归或栈

**时间复杂度**：O(V + E)

### 2. 广度优先搜索（BFS）

**核心思想**：按层次顺序访问顶点，先访问距离源点最近的顶点。

**实现方式**：队列

**时间复杂度**：O(V + E)

**应用**：无权图的最短路径

---

## 五、关键概念总结

| 算法 | 用途 | 时间复杂度 | 特点 |
|------|------|-----------|------|
| Prim | 最小生成树 | O(E + V log V) | 适合稠密图 |
| Kruskal | 最小生成树 | O(E log E) | 适合稀疏图 |
| Dijkstra | 单源最短路径 | O(E + V log V) | 边权非负 |
| Bellman-Ford | 单源最短路径 | O(VE) | 可处理负权边 |
| Floyd-Warshall | 多源最短路径 | O(V³) | 计算所有顶点对 |
| Kahn | 拓扑排序 | O(V + E) | DAG |
| DFS | 图遍历 | O(V + E) | 深度优先 |
| BFS | 图遍历 | O(V + E) | 广度优先，可求最短路径 |





# 图
## 最小生成树 plim' 克鲁斯卡尔
##  单元最短路径    拓扑排序




迪杰斯特拉
    min    访问   路径
0    0      T
1    4      T       0
2    5      T       1
3    6      T       0
4    10     T       5
5    9      T       2
6    16     T       4

0 1



    min    访问   路径
0    0      T
1    4      T      0
2    5      T      1 
3    6      T      0
4    10     T      5
5     9     T      2
6     16    T      4
7
8
0~6  16
0,1,2,5,4,6

    min   路径   访问   
0    0            T
1    -              T
2    10      1     T
3    50      4     T
4    30      0     T
5    60      3     T

0~5  60
0~2  10
0~3  50
0~4  30
0,4,3,5
