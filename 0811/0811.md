# C++ 面向对象编程

## 字符串与 vector

### 字符串
- C，char str[], char *
- C++ std::string

- 相等 ==
- 拼接 +, +=
- size、empty、at、

https://cplusplus.com/reference/string/string/

```c
    for (auto r : s1)           // r 局部变量，值拷贝；只读

    for (auto &r : s1)          // 引用，可以修改

    for (const auto &r : s1)    // 常量引用，不可以修改，只读，性能优

    for (auto &&r : s1)         // 万能引用，可以修改，**临时变量**可用

```
## 面向对象编程 - OOP

- OOA 分析
- OOD 设计
- OOP 编程

一种程序设计方法学，指导进行实践的理论或原则，编程范式

编程范式
- 面向过程：C、pascal
- 函数式编程：Lisp
- 面向对象：C++、Python

现实世界（问题）
    分析：
概念世界 - 模型
    设计或实现
程序世界

面向对象编程
- 对象：实体或概念在程序中投影，分离出关注点，一个模型描述

OOP 特性
- 封装
- 继承
- 多态

### 类与对象

类：对具有属性（名词）、行为（动词）的一组对象下的定义；模板
对象：具体的存在，类的实例

UML 统一建模语言
- 进行 OOA/OOD 一组图形化工具
- 建模、表示设计、构图

https://www.drawio.com/

```c
// Cat.h
#include <iostream>
#include <string>

using namespace std;

class Cat
{
private:
    string name;
    int age;
public:
    Cat();
    Cat(string name);
    Cat(string name, int age);
    ~Cat();

    string getName();

    int getAge();
    void setAge(int n);

    void show();
};

```

```c
// Cat.cxx
#include "Cat.h"

Cat::Cat()
{
    cout << "构造函数\n";
    name = "咪咪";
    age = 1;
}

Cat::Cat(string name)
{
    cout << "构造函数\n";
    this->name = name;
}

Cat::Cat(string name, int age)
{
    this->name = name;
    this->age = age;
}

Cat::~Cat()
{
    cout << "~ 析造函数\n";
}

string Cat::getName()
{
    return name;
}

int Cat::getAge()
{
    return age;
}

void Cat::setAge(int n)
{
    if (n > 0 && n < 25)
    {
        age = n;
    }    
}

void Cat::show()
{
    cout << "Cat: {名字：" << name << ", 年龄：" << age << "}" << endl;
}

```

```c
#include <iostream>

#include "Cat.h"

int main(int argc, char const *argv[])
{
    Cat c1;
    Cat c2("二狗");
    Cat c3("黑格尔", 19);

    c1.show();
    c2.show();
    c3.show();

    return 0;
}

```

## 作业
1. 定义一个类：point.h, point.cxx, main.cxx
    - Point
        int x
        int y
        Point()
        Point(int x)
        Point(int x, int y)
        ~Point()
        void show()
